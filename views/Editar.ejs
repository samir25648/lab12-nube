<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar producto</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
            background-color: #f9f9f9;
        }

        h1 {
            color: #393939;
            text-align: center;
            margin-bottom: 30px;
        }

        form {
            margin-bottom: 20px;
            text-align: center;
        }

        label {
            display: block;
            margin-bottom: 5px;
            color: #393939;
            font-weight: bold;
        }

        input[type="text"],
        input[type="file"] {
            width: 300px;
            padding: 10px;
            margin-bottom: 10px;
            border-radius: 4px;
            border: 1px solid #ccc;
        }

        button {
            padding: 10px 20px;
            background-color: #5709c4;
            color: #fff;
            border: none;
            cursor: pointer;
            border-radius: 4px;
        }

        a.btn {
            color: #5709c4;
            text-decoration: none;
            display: block;
            text-align: center;
            margin-top: 20px;
            padding: 10px 20px;
            border: 1px solid #5709c4;
            border-radius: 4px;
        }

        /* Nuevos estilos CSS */

        .container {
            max-width: 600px;
            margin: 0 auto;
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        .mb-3 {
            margin-bottom: 20px;
            text-align: left;
        }

        .form-control {
            display: block;
            width: 100%;
            padding: 10px;
            font-size: 16px;
            border-radius: 4px;
            border: 1px solid #ccc;
            transition: border-color 0.3s;
        }

        .form-control:focus {
            outline: none;
            border-color: #5709c4;
            box-shadow: 0 0 0 4px rgba(87, 9, 196, 0.1);
        }

        .btn-primary {
            background-color: #3014aa;
            color: #fff;
            transition: background-color 0.3s;
        }

        .btn-primary:hover {
            background-color: #5709c4;
        }
    </style>
</head>

<body>
    <div class="container">
        <h1>Editar producto</h1>
        <form method="post" action="/update/<%= data.id %>">
            <div class="mb-3">
                <label for="nombre">Nombre:</label>
                <input type="text" class="form-control" id="nombre" name="nombre" value="<%= data.nombre %>">
            </div>
            <div class="mb-3">
                <label for="descripcion">Descripción:</label>
                <input type="text" class="form-control" id="descripcion" name="descripcion" value="<%= data.descripcion %>">
            </div>
            <div class="mb-3">
                <label for="precio">Precio:</label>
                <input type="text" class="form-control" id="precio" name="precio" value="<%= data.precio %>">
            </div>
            <div class="mb-3">
                <label for="imagen">Imagen:</label>
                <input type="file" class="form-control" id="imagen" name="imagen" value="<%= data.imagen %>">
            </div>
            <button type="submit" class="btn btn-primary">Guardar</button>
        </form>
        <button class="btn btn-outline-primary" onclick="window.location.href = '/';">Volver al inicio</button>
    </div>
</body>

</html>
